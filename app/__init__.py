# ===========================================================================
# DIY Gift Basket API — Main Application Package
# ===========================================================================
# This package contains all modules for the FastAPI backend:
#   core/       — Configuration, security, and shared exceptions
#   db/         — Database engine, session factory, and base model
#   models/     — SQLAlchemy ORM models
#   schemas/    — Pydantic request / response schemas
#   repositories/ — Data-access layer (DB queries)
#   services/   — Business-logic layer
#   api/        — HTTP route handlers (thin controllers)
#   utils/      — Shared helper utilities
# ===========================================================================
